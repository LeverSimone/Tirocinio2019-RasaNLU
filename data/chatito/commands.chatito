import ./common/listcommon.chatito

// Go back to the previous page
%[go_back]('training': '20', 'testing': '10')
    ~[please?] ~[back]
    ~[please?] ~[return] ~[back]
    ~[please?] ~[return] ~[to the previous] ~[page]

~[return]
    go
    turn
    return

~[page]
    link
    page

// Go to the site
%[go_site]('training': '25', 'testing': '5')
    ~[please?] ~[open] ~[to?] @[site]

~[open]
    browse
    go
    search
    open

@[site]
    ~[cnn]
    ~[facebook]
    ~[audible]
    ~[twitter]
    ~[repubblica]
    ~[twitter barcellona]
    ~[google]